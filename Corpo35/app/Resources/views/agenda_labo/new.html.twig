{% extends 'base.html.twig' %}

{% form_theme form 'bootstrap_3_layout.html.twig' %}

{% block body %}
    <h1>Agenda_labo creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="{{ 'Créer'| trans }}" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('agenda_labo_index') }}">{{ 'Retour à la liste'| trans }}</a>
        </li>
        <li>
            <a  href="{{ path('index') }}">{{ 'Retour au site'| trans }}</a>
        </li>
    </ul>
{% endblock %}
