{% extends 'base.html.twig' %}

{% block body %}
    <h1>Vote creation</h1>

    {{ form_start(form) }}
        <div class="form">{{ form_row(form.note) }}</div>
        <div class="form">{{ form_row(form.candidat) }}</div>
        <div class="form">{{ form_row(form.jury) }}</div>
        <div class="form">{{ form_row(form.commentaire) }}</div>

        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('vote_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
