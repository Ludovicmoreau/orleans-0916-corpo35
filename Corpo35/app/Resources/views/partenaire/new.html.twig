{% extends 'BackBundle:Default:backLayout.html.twig' %}

{% block stylesheet %}
    {{ parent() }}
    <link rel="stylesheet" href="{{ asset('css/back.css') }}">
{% endblock %}

{% block content %}

    {% form_theme form'bootstrap_3_layout.html.twig' %}

    <div class="container admin">


        <h2>Partenaire {% if is_granted ('ROLE_ADMIN') %} <a href="{{ path('partenaire_index') }}"> / retour à la liste</a>{% endif %}  </h2>        <h2 style="font-size: 1.3em">Attention pour le logo, ne pas mettre des images de grandes tailles et les photos ne doivent pas dépasser 2Mo.</h2>


        {{ form_start(form) }}
            {{ form_widget(form) }}
            {#<input type="submit" value="{{ 'Créer'| trans }}" />#}
        <button class="btn btn-success" type="submit"><i class="fa fa-check" aria-hidden="true"></i></button>
        {{ form_end(form) }}

    </div>

{% endblock %}


{#<a  href="{{ path('index') }}">{{ 'retour au site'| trans }}</a>#}