{% extends 'BackBundle:Default:backLayout.html.twig' %}

{% block content %}
    <h1>Validation creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('validation_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
